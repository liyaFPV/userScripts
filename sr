#скрипт для чтения текста в терминале
#!/bin/bash

DIR="$(dirname "$(realpath "$0")")"

# значение по умолчанию
DEFAULT_WPM=300

# если второй параметр не передан — используем значение по умолчанию
WPM="${2:-$DEFAULT_WPM}"

"$DIR/speedread-1.0/speedread" -w "$WPM" < "$1"
